# Trivy findings accepted on purpose (https://trivy.dev/latest/docs/configuration/filtering/).
# Every entry needs a `statement` explaining why it is safe or unfixable and an
# `expired_at` date so it is reviewed again, for example:
#
# vulnerabilities:
#   - id: CVE-2026-00000
#     paths:
#       - usr/local/bin/example
#     statement: Unreachable code path; upstream fix pending in example v1.2.
#     expired_at: 2026-12-31
vulnerabilities: []
secrets: []
