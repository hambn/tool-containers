# Podman

Run `./run.sh` for a rootless interactive shell with the current directory mounted at
`/workspace`. Set `AGENTBLOAT_IMAGE=ghcr.io/hambn/agentbloat:<variant>` to select a
variant. The `:Z` mount option supports SELinux hosts.
