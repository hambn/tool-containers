# Podman

Run `./run.sh` for a rootless Pi session with the current directory mounted at
`/workspace`. Set `PI_AGENT_IMAGE=ghcr.io/hambn/pi-agent:<variant>` to select a
variant; the `:Z` mount option supports SELinux hosts.
