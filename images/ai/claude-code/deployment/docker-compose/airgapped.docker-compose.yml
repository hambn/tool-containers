# Offline: build from the repo's Dockerfile instead of pulling a registry image.
# Run from repo root so the build context resolves:
#   docker compose -f images/ai/claude-code/deployment/docker-compose/airgapped.docker-compose.yml run --rm claude
name: claude-code

services:
  claude:
    build:
      context: ../../images/ubuntu-browser
    image: claude-code:airgapped
    pull_policy: never
    container_name: claude-code
    hostname: claude-code
    networks: [claude-code]
    environment:
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:?set ANTHROPIC_API_KEY}
    volumes:
      - ${WORKSPACE:?Set WORKSPACE to an absolute host path}:/workspace
    stdin_open: true
    tty: true

networks:
  claude-code:
    name: claude-code
