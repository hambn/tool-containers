# Podman

Run `./run.sh` for a rootless Codex session with the current directory mounted at
`/workspace`. Set `CODEX_IMAGE=ghcr.io/hambn/codex:<variant>` to select a variant;
the `:Z` mount option supports SELinux hosts.
