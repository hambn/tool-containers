# docker compose run --rm agentimg
name: agentimg

services:
  agentimg:
    image: ${AGENTIMG_IMAGE:-ghcr.io/hambn/agentimg:latest}
    command: ["zsh", "-l"]
    volumes:
      - ./:/workspace
    stdin_open: true
    tty: true
    networks: [agentimg]

networks:
  agentimg:
    name: agentimg
