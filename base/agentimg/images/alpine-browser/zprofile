# Restore agentimg tool paths after distribution login profiles replace ENV PATH.
typeset -U path PATH
path=("$HOME/.local/bin" /usr/local/bin /usr/local/go/bin $path)
[[ -d /headless-shell ]] && path=(/headless-shell $path)
export PATH
